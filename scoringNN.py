import keras
import tensorflow
from keras.model import Sequential
from keras.layers import Dense, Dropout

scoringModel = Sequential()

scoringModel.add(Dense())
scoringModel.add(Dropout())
scoringModel.add(Dense())
scoringModel.add(Dropout())
scoringModel.add(Dense())
scoringModel.add(Dropout())

